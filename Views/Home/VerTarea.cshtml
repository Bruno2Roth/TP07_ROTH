@{
    ViewData["Title"] = "Lista de Tareas";
    var tareas = ViewBag.Tareas as List<TP07_ROTH.Models.Tarea>;
}

<div class="container mt-4">
    <h2>Mis Tareas</h2>
    @if (tareas != null && tareas.Count > 0)
    {
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Título</th>
                    <th>Descripción</th>
                    <th>Fecha</th>
                    <th>Finalizada</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var t in tareas)
                {
                    <tr>
                        <td>@t.Titulo</td>
                        <td>@t.Descripcion</td>
                        <td>@t.Fecha.ToString("dd/MM/yyyy")</td>
                        <td>@(t.Finalizada ? "Sí" : "No")</td>
                        <td>
                            <a class="btn btn-warning btn-sm" href="/Home/ModificarTarea">Editar</a>
                            <a class="btn btn-success btn-sm" href="/Home/FinalizarTarea">Finalizar</a>
                            <a class="btn btn-danger btn-sm" href="/Home/EliminarTarea">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No tienes tareas registradas.</p>
    }
</div>
